<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Hospital Registration - HealthAlert</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Roboto&family=Ubuntu:wght@400;500;700&display=swap" rel="stylesheet" />
  <style>
    body { font-family: 'Ubuntu', sans-serif; }
  </style>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8 bg-white p-8 sm:p-10 rounded-xl border border-gray-200 shadow-xl">
    <div class="text-center">
      <div class="w-16 h-16 bg-red-500 rounded-full mx-auto flex items-center justify-center">
        <i class="fas fa-hospital text-white text-2xl"></i>
      </div>
      <h2 class="mt-6 text-3xl font-extrabold text-gray-900">Register Hospital</h2>
      <p class="mt-2 text-sm text-gray-600">Join the HealthAlert network</p>
    </div>

    <% if (errors.length > 0) {%>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
      <strong class="font-bold">Error!</strong>
      <span class="block sm:inline">
        <% errors.forEach(error => { %>
          <li><%= error %></li>
        <% }) %>
      </span>
      <span class="absolute top-0 bottom-0 right-0 px-4 py-3">
        <i class="fas fa-times"></i>
      </span>
      </div>
    <% } %> 

    <form action="/hospital-register" method="POST" class="mt-8 space-y-6">
      <div class="space-y-4">
        <!-- Hospital Name -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Hospital Name</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-hospital text-red-500"></i>
            </div>
            <input id="name" name="name" type="text" required
              value="<%= oldInput.name ? oldInput.name : '' %>"
              class="w-full px-10 py-3 border border-gray-300 text-gray-900 rounded-lg focus:outline-none focus:ring-red-500 focus:border-red-500 placeholder-gray-500"
              placeholder="Enter hospital name" />
          </div>
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Hospital Email</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-envelope text-red-500"></i>
            </div>
            <input id="email" name="email" type="email" required
              value="<%= oldInput.email ? oldInput.email : '' %>"
              class="w-full px-10 py-3 border border-gray-300 text-gray-900 rounded-lg focus:outline-none focus:ring-red-500 focus:border-red-500 placeholder-gray-500"
              placeholder="hospital@example.com" />
          </div>
        </div>

         <!-- Password -->
        <div>
          <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-lock text-red-500"></i>
            </div>
            <input id="password" name="password" type="password" required minlength="8"
              value="<%= oldInput.password ? oldInput.password : '' %>"
              class="w-full px-10 py-3 border border-gray-300 text-gray-900 rounded-lg focus:outline-none focus:ring-red-500 focus:border-red-500 placeholder-gray-500"
              placeholder="Create a secure password" />
            <button type="button" class="absolute inset-y-0 right-0 pr-3 flex items-center" onclick="togglePassword()">
              <i id="password-toggle" class="fas fa-eye text-gray-400 hover:text-red-500"></i>
            </button>
          </div>
          <p class="text-xs text-gray-500 mt-1">Password must be at least 8 characters long</p>
        </div>
      </div>

        <!-- Phone Number -->
        <div>
          <label for="phone_no" class="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-phone text-red-500"></i>
            </div>
            <input id="phone_no" name="phoneNo" type="tel" required pattern="[0-9]{10,15}"
              value="<%= oldInput.phoneNo ? oldInput.phoneNo : '' %>"
              class="w-full px-10 py-3 border border-gray-300 text-gray-900 rounded-lg focus:outline-none focus:ring-red-500 focus:border-red-500 placeholder-gray-500"
              placeholder="e.g. 9876543210" />
          </div>
        </div>

        <!-- Location -->
        <div>
          <label for="location" class="block text-sm font-medium text-gray-700 mb-1">Hospital Location</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-map-marker-alt text-red-500"></i>
            </div>
            <input id="location" name="location" type="text" required
              value="<%= oldInput.location ? oldInput.location : '' %>"
              class="w-full px-10 py-3 border border-gray-300 text-gray-900 rounded-lg focus:outline-none focus:ring-red-500 focus:border-red-500 placeholder-gray-500"
              placeholder="Enter hospital address" />
          </div>
        </div>

       

      <!-- Terms and Conditions -->
      <!-- <div class="flex items-start">
        <input id="terms" name="terms" type="checkbox" required
          class="h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 rounded mt-1" />
        <label for="terms" class="ml-2 block text-sm text-gray-900">
          I agree to the <a href="/terms" class="text-red-600 hover:underline">Terms and Conditions</a> 
          and <a href="/privacy" class="text-red-600 hover:underline">Privacy Policy</a>
        </label>
      </div> -->

      <!-- Submit -->
      <div>
        <button type="submit"
          class="w-full flex justify-center items-center gap-2 py-3 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-red-500 hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition duration-300 transform hover:scale-105">
          <i class="fas fa-hospital"></i>
          Register Hospital
        </button>
      </div>

      <!-- Already have account -->
      <div class="text-center mt-6">
        <p class="text-sm text-gray-600">
          Already registered?
          <a href="/hospital-login" class="font-medium text-red-600 hover:text-red-500 transition duration-300">Log in here</a>
        </p>
      </div>

      <!-- User Portal Link -->
      <div class="text-center pt-4 border-t border-gray-200">
        <p class="text-xs text-gray-500">
          Individual user? 
          <a href="/user-register" class="font-medium text-gray-600 hover:text-red-500">
            Register as user instead
          </a>
        </p>
      </div>
    </form>
  </div>

  <script>
    function togglePassword() {
      const passwordInput = document.getElementById('password');
      const toggleIcon = document.getElementById('password-toggle');
      
      if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        toggleIcon.classList.remove('fa-eye');
        toggleIcon.classList.add('fa-eye-slash');
      } else {
        passwordInput.type = 'password';
        toggleIcon.classList.remove('fa-eye-slash');
        toggleIcon.classList.add('fa-eye');
      }
    }
  </script>
</body>
</html>